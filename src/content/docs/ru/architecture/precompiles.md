---
title: "Precompile-контракты: расширение 2D без EVM"
description: Любой сети рано или поздно нужна прикладная логика сверх обычных переводов. 2D вместо EVM берёт короткий список Elixir-модулей, каждый из которых помещается в один файл и читается от начала до конца. Этот дизайн открывает двери, куда wrapped-мост безопасно зайти не может.
---

Сеть, которая умеет только двигать деньги, мало что может. Кредитование, эскроу, свопы, авторизованные записи оракулов, атомарные вводы и выводы; любому серьёзному стеку нужны контракты на цепи сложнее «списать у A, зачислить B».

Стандартный ответ индустрии: **EVM**, универсальный интерпретатор байт-кода, выполняющий произвольный код, который любой может задеплоить. Цена известна. Газ-метринг, язык, построенный вокруг этого газ-метринга, поверхность атаки самой виртуальной машины, и открытый вопрос, что происходит, когда недоверенный код делает с состоянием что-то неожиданное.

2D пошла другим путём: **компактный явный реестр precompile-контрактов**. Каждый «контракт» это Elixir-модуль, который помещается в один файл и читается от начала до конца. Он реализует фиксированный behaviour и живёт по фиксированному адресу в пространстве имён `0x2D00…`. Нет интерпретатора байт-кода, нет слоя газ-метринга, в котором можно ошибиться. Набор кода закрыт: precompile-контракты выкатывает сам оператор узла, поэтому заранее известно, что именно выполняется.

В статье разбираем по порядку: как транзакция находит precompile, что `@behaviour Chain.Precompile` требует от реализации, где живёт реестр, как выглядит рабочий HTLC precompile и почему эта модель строже wrapped-моста.

## Как транзакция находит precompile

У каждой транзакции есть `to`-адрес. Производитель блока идёт по основному пути: декодирует подписанную транзакцию, проверяет nonce, затем спрашивает у реестра precompile-ов, принадлежит ли `to` зарегистрированному обработчику. Нашли обработчик: диспатчим в его `execute/3`. Не нашли: проваливаемся в нативный USDC-перевод. На второй путь приходится 99% трафика, потому что обычный перевод между аккаунтами **не** precompile, это дефолт.

Код диспатча ([`lib/chain/block_executor.ex:284-318`](https://github.com/igor53627/2d/blob/4d955b70efde1075e316d9ab2c2c10820fb0cd71/lib/chain/block_executor.ex#L284-L318)):

```elixir
defp charge_and_execute(pending, tx, from, to, value, fee, block_number, tx_index, ts) do
  case to && Chain.Precompiles.Registry.lookup(to) do
    {:ok, handler} ->
      cond do
        Decimal.compare(value, Decimal.new(0)) != :eq ->
          {:error, :precompile_value_not_supported}

        true ->
          with {:ok, input} <- Crypto.decode_hex_safe(tx.input),
               true <- byte_size(input) >= 4 do
            <<selector::binary-4, args::binary>> = input
            run_precompile_execute(handler, selector, args, input, from, value, ...)
          else
            _ -> {:error, :invalid_calldata}
          end
      end

    _ ->
      # нативный USDC-перевод: списать с отправителя, зачислить получателю, взять комиссию.
      ...
  end
end
```

Две ветки, одно решение. Зарегистрирован? Вызываем обработчик. Не зарегистрирован? Двигаем USDC. Ничего другого как кастомное выполнение не проходит; всё кастомное идёт через precompile.

## Behaviour `Chain.Precompile`

Каждый precompile реализует три колбэка ([`lib/chain/precompiles/precompile.ex`](https://github.com/igor53627/2d/blob/4d955b70efde1075e316d9ab2c2c10820fb0cd71/lib/chain/precompiles/precompile.ex)):

```elixir
defmodule Chain.Precompile do
  @callback address() :: binary()

  @callback execute(selector :: binary(), args :: binary(), context :: Chain.Precompile.Context.t()) ::
              {:ok, result :: binary(), logs :: list()} | {:revert, reason :: binary()}

  @callback read(selector :: binary(), args :: binary()) ::
              {:ok, abi_encoded :: binary()} | {:revert, reason :: binary()}
end
```

- `address/0`. Где живёт этот precompile. Захардкожен в диапазоне `0x2D00…`, чтобы весь набор легко перечислить.
- `execute/3`. Точка входа, меняющая состояние. Вызывается внутри транзакции базы блока. Возвращает result + список логов, которые попадают в tx-receipt, либо `{:revert, reason}` для чистого отката.
- `read/2`. View-вызов. Используется в `eth_call` и `triggerconstantcontract`. Состояние не меняет.

`context`-структура, передаваемая в `execute/3`, несёт то, что в Solidity называется `msg.sender` и `msg.value`, плюс высоту блока, timestamp блока и tx-index. Всё, что нужно обработчику, чтобы понять, от чьего имени он действует. Узость этого интерфейса сделана сознательно: `execute/3`, который умеет только явно писать аккаунты и таблицы через локальные модули, ревьюить сильно проще, чем целую EVM.

## Реестр

Реестр ([`lib/chain/precompiles/registry.ex`](https://github.com/igor53627/2d/blob/4d955b70efde1075e316d9ab2c2c10820fb0cd71/lib/chain/precompiles/registry.ex)) это ETS-таблица, ключом является адрес:

```elixir
def lookup(address) when is_binary(address) do
  case :ets.lookup(@table, address) do
    [{^address, module}] -> {:ok, module}
    [] -> :not_found
  end
end

def register(address, handler_module) do
  GenServer.call(__MODULE__, {:register, address, handler_module})
end
```

При запуске узел загружает активные precompile-контракты один раз, а каждая транзакция дальше платит только за ETS-lookup. Единицы микросекунд. Добавить precompile: один вызов `register/2` плюс деплой модуля. Никакого байт-кода через mempool: операторы выкатывают precompile-контракты тем же способом, каким выкатывают остальной код узла.

Пространство имён адреса тоже имеет значение. Все системные precompile-контракты живут по `0x2D00…`; верхний байт `0x2D` (ASCII `-`, от названия проекта) это зарезервированный префикс. `eth_getCode` возвращает `0x01` для любого адреса в этом диапазоне, который резолвится, так что кошельки, которые перед вызовом проверяют через `eth_getCode`, контракт ли это, получают правильный ответ без необходимости иметь полную EVM.

## HTLC precompile

Пора от абстракций к коду. HTLC precompile по адресу `0x2D00…0001` это уже рабочий код цепи с состоянием ([`lib/chain/precompiles/htlc.ex`](https://github.com/igor53627/2d/blob/4d955b70efde1075e316d9ab2c2c10820fb0cd71/lib/chain/precompiles/htlc.ex)).

HTLC (hashed time-locked contract) это примитив, лежащий в основе атомарных свопов на цепи. Alice лочит средства с хэшем `H` и deadline. Любой, кто знает preimage `P` такой, что `sha256(P) = H`, может забрать средства до истечения deadline. Если никто не забрал, Alice делает refund себе. Магия в том, что две стороны на двух разных сетях могут запустить парные HTLC-ы и получить своп, который либо полностью завершается (preimage раскрывается с обеих сторон), либо полностью откатывается (обе deadline-ы истекают, обе стороны получают refund). Доверять некому: ни посреднику, ни валидаторной федерации, ни единому контракту, где лежат все заблокированные средства.

```elixir
defmodule Chain.Precompiles.HTLC do
  @moduledoc """
  Hashed time-locked contract: атомарные свопы без доверенного моста.
  """
  @behaviour Chain.Precompile

  @address <<0x2D, 0::144, 0x01>>  # 0x2D00…0001

  # 4-байтные Keccak-256-селекторы
  @lock_selector selector("lock(bytes32,address,uint256,uint256)")
  @claim_selector selector("claim(bytes32,bytes32)")
  @refund_selector selector("refund(bytes32)")
  @get_swap_selector selector("getSwap(bytes32)")

  defp selector(signature), do: :erlang.binary_part(ExKeccak.hash_256(signature), 0, 4)

  @impl true
  def address, do: @address

  @impl true
  def execute(
        @lock_selector,
        <<hash::binary-32, _pad::binary-12, receiver::binary-20,
          amount::256, deadline_ms::256>>,
        ctx
      ) do
    do_lock(hash, receiver, amount, deadline_ms, ctx)
  end

  def execute(@claim_selector, <<hash::binary-32, preimage::binary-32>>, ctx) do
    do_claim(hash, preimage, ctx)
  end

  def execute(@refund_selector, <<hash::binary-32>>, ctx) do
    do_refund(hash, ctx)
  end

  def read(@get_swap_selector, <<hash::binary-32>>), do: do_get_swap(hash)
end
```

Полный обработчик всё ещё достаточно мал, чтобы ревьюить его напрямую. Четыре селектора, одна строка `state.htlc_swaps` на hash, строгие предусловия на каждой ветке и все изменения балансов внутри SERIALIZABLE-транзакции блока. Никакого reentrancy, скрытых циклов и сюрпризов с газом.

Мост использует ту же модель precompile-а на `0x2D00…0003`: [`BridgeRefillMint`](https://github.com/igor53627/2d/blob/4d955b70efde1075e316d9ab2c2c10820fb0cd71/lib/chain/precompiles/bridge_refill_mint.ex) отдаёт только `bridge_lock(...)`, атомарно вставляет `bridge_mints` и привязанный к получателю HTLC, а межсетевое доказательство оставляет верификатору. Подробный поток описан в [статье про мост](../bridge/).

## Чем это лучше моста

Обычный способ перенести активы между сетями это **wrapped-мост**. Alice отправляет USDC в custody-контракт на сети A, федерация валидаторов наблюдает лок, и на сети B для неё минтится wrapped-представление. Знакомая, широко развёрнутая и катастрофически уязвимая схема: **с 2020 года из cross-chain мостов украдено более $2.8B, это примерно 40% всего объёма украденного в Web3** ([Chainalysis / отраслевой обзор](https://www.certik.com/resources/blog/GuBAYoHdhrS1mK9Nyfyto-cross-chain-vulnerabilities-and-bridge-exploits-in-2022)). Один только 2026 уже зафиксировал **больше $750M потерь на мостах менее чем за четыре месяца** ([Phemex: DeFi-хаки 2026](https://phemex.com/blogs/defi-hacks-2026-bridge-exploits-explained)).

Громкие провалы читаются как каталог компрометаций валидационного слоя:

- **Ronin** (2022, ~$620M). Фишинг пяти из девяти валидаторных ключей; атакующий подписал два огромных вывода.
- **Wormhole** (2022, ~$320M). Некорректное использование Solana-helper-а позволило принять подделанную guardian-подпись; wETH заминтился из воздуха.
- **Nomad** (2022, ~$190M). Апгрейд случайно обошёл критическую проверку, мост превратился в «бери кто хочешь».
- **Poly Network** (2021, ~$611M). Функция `lock` межсетевого manager-а позволяла выдернуть unlock произвольной суммы.

HTLC precompile не имеет федерации валидаторов, которую можно скомпрометировать, не имеет подписи, которую можно подделать, не имеет апгрейд-пути, в котором можно ошибиться, и не собирает TVL в одном контракте, который становится очевидной мишенью. Вся его модель доверия: «ты знаешь preimage» плюс «на этой сети существует deadline».

| | Wrapped-мост (Wormhole / Ronin / Nomad / …) | HTLC precompile |
|---|---|---|
| Модель доверия | N-of-M федерация валидаторов, кастодиан, или оракул | `sha256(preimage) = hash` + on-chain deadline |
| Успешный путь | Кастодиан одобряет wrap/unwrap | Раскрытие preimage, деньги едут |
| Путь отказа | Компрометация валидаторов сливает весь пул | Deadline истекает, обе стороны делают refund |
| Концентрация TVL | Да. Миллиарды в одном контракте | Нет. Каждый lock независим, ограничен своей суммой |
| Риск апгрейда | Multisig может апгрейдить (ещё один вектор атаки) | Деплой обработчика это обычный operator-релиз; адрес фиксирован |
| Что лежит на цепи | Контракт, который доверяет off-chain-подписантам | Весь автомат состояний, каждая ветка |
| Украдено с 2020 | $2.8B+ | Худший случай это один пользователь, теряющий один lock |

Последняя строка главная. Баг в мосту по природе *системный*: одна ошибка сливает весь пул. Отказ HTLC *per-swap*: одна сторона может не успеть claim или refund, и теряет максимум то, что положила в конкретный lock. Нет модели угроз «слить весь мост», потому что никакого моста нет.

Дизайн precompile-а также обходит одну тонкую проблему мостовых свопов. **Мост это смарт-контракт, а смарт-контракты на виртуальной машине безопасны ровно настолько, насколько безопасны их реализация и upgrade-контролы**. Precompile это Elixir-модуль, отревьюенный перед отгрузкой; его upgrade-путь это релиз узла, не multisig-транзакция; layout состояния статически известен в таблицах Postgres. Поверхность атаки смещается с вопроса «делает ли контракт то, что написано в whitepaper» к вопросу «соответствует ли Elixir-код спецификации», и это уже задача, которую формальные методы умеют решать в промышленном масштабе.

## Компромиссы относительно EVM

Precompile-модель не бесплатный обед. Она реально отказывается от части свойств:

- **Нет деплоя от третьих лиц.** Нельзя задеплоить новый precompile, отправив байт-код в mempool. Добавление precompile это operator-релиз, как и любая другая фича узла. Это фича, если нужен curated-набор кода, и баг, если нужна открытая permissionless-платформа.
- **Меньше выразительности.** Precompile это один Elixir-модуль с фиксированной точкой входа. Нельзя построить ad-hoc композабилити двух недоверенных контрактов, как это делается на EVM. Если двум precompile-ам нужно взаимодействовать, это взаимодействие пишет оператор в Elixir.
- **Меньше экосистема.** Весь dApp-стек в EVM-совместимых сетях построен вокруг Solidity. 2D работает иначе: интегрирующимся приложениям надо идти к ABI каждого precompile-а напрямую, готовый Solidity-тулчейн из коробки не подойдёт.

Что получаем взамен:

- **Каждый путь кода на цепи это Elixir, который можно прочитать и проверить.** Типизирован, тестируем, запускается в REPL, ограничен размером файла модуля.
- **Нет газ-метринг-сложности.** Газ-метринг существует, чтобы останавливать недоверенный код, крутящийся бесконечно. Precompile-код доверенный, ему не нужен per-opcode-метр. Благодаря этому газ-модель всей сети может быть сильно проще: не нужно per-opcode-бухгалтерии, потому что нет adversarial-опкодов.
- **Нулевая поверхность атаки VM.** Нет EVM-багов, нет байт-код-эксплойтов, нет тонких уязвимостей на взаимодействии опкодов. Поверхность атаки это несколько precompile-модулей плюс сам BEAM. Существенное сокращение.

## Стратегия безопасности

Закрытый, проверяемый набор precompile-ов открывает двери в верификации кода, которые действительно трудно открыть, когда набор кода это «что угодно, что кто угодно задеплоит»:

- **Tier 0 (baseline, сегодня).** Dialyzer + set-theoretic types Elixir 1.20 дают precompile-модулям реальные статические гарантии. Спеки на колбэки `@behaviour`-а уже отсеивают целый класс багов на malformed input.
- **Tier 1.** Property-based тесты через PropCheck, гоняющие `execute/3` случайными парами selector+args против module-level инвариантов («сумма балансов сохраняется», «lock существует тогда и только тогда, когда он в store»).
- **Tier 2 (конкурентность).** Concuerror перебирает все возможные порядки операций ETS между реестром, обработчиком и производителем блоков. Находит гонки и deadlock-и, которые обычные тесты не ловят.
- **Tier 3 (протокол).** TLA+ спеки (генерируемые в запускаемый Erlang через Erla+) для precompile-ов, корректность которых это многошаговый протокол, а не одна функция. Канонический target: HTLC с инвариантом «completed XOR rolled_back, никогда частично».

Каждый ярус дороже и сильнее предыдущего. Смысл в том, чтобы он собирался постепенно, по мере того как появляются настоящие precompile-ы, а не авансом.

## Куда это ведёт

Первые precompile-контракты с реальным состоянием уже живые: HTLC-расчёт и bridge refill/mint. Следующий шаг: усиливать стек верификации вокруг них. Dialyzer и типы уже в CI, дальше идут точечные PropCheck-свойства для обработчиков с состоянием и TLA+ спеки для HTLC плюс следующих precompile-ов, где корректность является протоколом, а не одной функцией.
